@model IEnumerable<Jazzima1.Models.SavedAlbum>

@{
    ViewData["Title"] = "Index";
}

<div class="savedalbums">

<div class="table">
    @*<thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Album.Image)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>*@
@foreach (var item in Model) {
        <div>
            <a class="album-button-card">
                <img class="img-thumbnail" style="max-width: 150px;" src="@item.Album.Image" alt="" />
                <a asp-action="Details" asp-route-id="@item.Id" asp-controller="Albums">
                    <input class="btn btn-success" type="submit" value="Details" />
                </a>
                <a asp-controller="SavedAlbums" asp-action="Delete" asp-route-id="@item.Id">
                    <input class="btn btn-warning" type="submit" value="Delete" />
                </a>
           
            @*<td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>*@
        </div>
}
    </div>
</div>
</div>